# 2026-06-01

## SRA_Accessions 并行审计产物同步（commit 4a81f9d）

- 从 4090 服务器 `/home/m252202014/` 拉取两个审计目录到 `E:\TSS_doc`：
  - `sra_accessions_audit_20260601/`（普通版，280K，16 分片，对应 NCBI_SRA_Metadata_20260516）
  - `sra_accessions_audit_full_20260601/`（Full 版，228K，32 分片，对应 NCBI_SRA_Metadata_Full_20260516）
- 两个版本均处理 1.48 亿行级 SRA_Accessions 元数据，产出 Accession 前缀/Type/Status/Visibility/关键字段非缺失计数等统计。
- 工具链：C++ 分片审计（`sra_accessions_chunk_audit.cpp`）+ Python 合并（`merge_sra_accessions_chunks.py`）+ Shell 并行调度（`run_sra_accessions_parallel.sh`），统一组织 `code/`、`logs/`、`outputs/` 三层结构。
- 推送到 `origin/DSS`（commit `4a81f9d`）。
- 传输方式：`scp -P 24722 -r` 直连 4090 服务器（284K + 236K，秒级完成，无需 tar 打包）。
- Windows 端 `git add` 输出大量 `LF will be replaced by CRLF` 警告，属正常现象，提交不受影响。

## 关键记忆

- **4090 服务器 SSH**：`ssh -p 24722 m252202014@223.2.43.222`，项目根目录 `/home/m252202014/`。
- **GitHub 远程仓库**：`https://github.com/Hongxuan-Wu/transcriptomics-pipelines.git`（分支 DSS，已通过 PAT 认证）。
- **Git 仓库根目录**：`E:\TSS_doc`，远程 `origin/DSS` 已同步。
- 当前文件操作习惯：用户要求"传文件夹"时默认走 `scp -r` 全量拉取，不打包；commit 信息使用 `-m` 多次分段写法，结构化呈现。
