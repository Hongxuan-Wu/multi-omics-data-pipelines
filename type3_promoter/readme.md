# type3_promoter——III 型启动子数据挖掘流程

本目录目前为占位目录。后续如果 III 型启动子分析需要复用 SRA/RefSeq/TSS 数据索引，应明确：

- 输入数据来自哪个索引或下载结果。
- 筛选标准是什么。
- 输出表和 QC 如何定义。
- 与 `tss/` 目录的边界是什么。

## 目录结构

```text
.
└── readme.md
```


如果我们从湿实验的角度出发，一开始就关注Ⅲ型启动子在crisper系统中对表达的精细调控，那么就可以逆向推导需求：我们需要一个能对启动子的位置极为敏感的模型，这对于分词和建模都有极高的要求。
